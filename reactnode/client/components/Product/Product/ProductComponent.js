{/*import React,{Component} from 'react';
import CartImage from '../AddTocart/CartImage';
import ProductDetail from '../AddTocart/ProductDetail';

class Product extends Component {
    render() {
        return (
               
                <div className="product">
                    
                <div className="productImage">
                   <CartImage images={this.props.product.url}/>
                   </div>
               
                    <div className="productDetail">
                    <ProductDetail 
                     prodName={this.props.product.name}
                     prodCost={this.props.product.cost} 
                     currency={this.props.currency
                     }/>
                </div>
                {/*<button title={this.props.product.id}
                onClick={this.props.AddToCart}>AddToCart</button>
                <AddToCart stock={this.props.product.stock}/>=====use comment here====
            </div>          
        );

    }

}
export default Product;*/}